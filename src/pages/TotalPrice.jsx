import React from 'react';

const TotalPrice = ({ totalPrice }) => {
    return (
      <div>
        <h2 id="set">Total Price: ${totalPrice}</h2>
      </div>
    );
  };

export default TotalPrice;
